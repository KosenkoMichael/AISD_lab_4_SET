#pragma once

#include <string>
#include <iostream>
#include <random>
#include <exception>

using namespace std;
	string generate_random_string(int length);
	int generate_random_number(int min, int max);
	namespace set {

	}